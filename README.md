# imdb-top-250-etl-pipeline
Dockerized ETL pipeline that scrapes IMDb’s Top 250 movies using Selenium and BeautifulSoup, normalizes movie and cast data, and loads it into a PostgreSQL database. Includes cron scheduling, upsert logic, and a fully relational schema.
